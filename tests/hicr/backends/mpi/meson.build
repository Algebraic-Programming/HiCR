# Registering tests
if 'mpi' in enabledBackends
	#test('mpi', executable('mpi', 'mpi.cpp', dependencies: [ testDeps, hicrMPIBackendDep ] ), protocol: 'gtest', suite: [ 'hicr', 'backends', 'mpi' ] )
endif

