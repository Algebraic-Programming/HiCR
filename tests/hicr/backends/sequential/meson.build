# Registering tests
test('sequential', executable('sequential', 'sequential.cpp', dependencies: [  testDeps, hicrSequentialBackendDep ] ), protocol: 'gtest', suite: [ 'hicr', 'backends', 'sequential' ] )
test('process', executable('process', 'process.cpp', dependencies: [  testDeps, hicrSequentialBackendDep ] ), protocol: 'gtest', suite: [ 'hicr', 'backends', 'sequential' ] )
