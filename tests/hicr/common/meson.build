# Registering Task class tests
test('coroutine', executable('couroutine', files(['coroutine.cpp']), dependencies: [ testDeps, dependency('threads', required: true) ], ), protocol: 'gtest', suite: [ 'tests', 'hicr', 'common' ])

