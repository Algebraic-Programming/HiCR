image: alpine

pages:
  tags:
  - docker
  - tiny 
  script:
  - apk update && apk add doxygen
  - cd docs && doxygen Doxyfile
  - mv docs/doxygen/html/ public/
  artifacts:
    paths:
    - public
  only:
  - master
